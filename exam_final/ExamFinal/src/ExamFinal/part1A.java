package ExamFinal;

public class part1A {
	
	public part1A() { }
	
	// STATEMENT
	/*
	 * (a) Dans l’exemple du reseau d’activites stochastique des notes de
	 * cours (Voir les exemples 1.4, 1.20, 1.47, 1.49, 1.50, 6.3, 6.21, and
	 * 6.36), combinez la methode CMC utilisee dans l’exemple 6.21 avec
	 * l’utilisation de variables de controle, comme suit. Prenez chaque Y_j ∈
	 * B comme variable de controle, pour estimer μ = μ 2 = P[T > x] pour x =
	 * 90. Prenez n = 10000.
	 * Estimez le vecteur β∗ des huit coefficients optimaux sans faire d’experience
	 * pilote, estimez la variance de l’estimateur de μ, et calculez un intervalle
	 * de confiance pour μ, avec et sans les variables de controle. 
	 * Discutez et comparez.
	 * 
	 * Pour les operations matricielles servant a calculer votre estimateur de β∗,
	 * vous pouvez utiliser cern.colt.matrix.linalg.Algebra dans la librairie Colt
	 * (voir le site web de SSJ). Le programme CallCenterWithCV.java sur la page
	 * du cours est un exemple de programme utilisant cette librairie (dans la 
	 * methode ApplyCV). Il existe aussi plusieurs autres librairies d’algebre
	 * lineaire en Java (ou autre) que vous pouvez utiliser.
	 */
	

}
